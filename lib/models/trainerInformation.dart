class TrainerInformation{

  String trainerName;
  String trainerAge;
  String homeTown;
  String trainerType;
  String trainerID;
  String gender;
  String favoritePokemon;
  String trainerEmail;
  String contactNumber;

  TrainerInformation(this.trainerName, this.trainerAge, this.homeTown, this.trainerType, this.trainerID, this.gender, this.favoritePokemon, this.trainerEmail, this.contactNumber);
}